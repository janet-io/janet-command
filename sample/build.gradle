apply plugin: 'application'

mainClassName = 'io.janet.command.sample.CommandSample'

compileJava {
    sourceCompatibility = '1.8'
    targetCompatibility = '1.8'
}

dependencies {
    implementation project(':service')
}